query GetCollection($slug: String!) {
    collection(slug: $slug) {
        id
        name
        slug
        description
        products {
            id
            slug
            name
            price
            rating
            images {
                url
                alt
            }
        }
    }
}
